import * as React from 'react';
import { Text,
    View,
    SafeAreaView,
    StatusBar }
    from 'react-native';

export default class Message extends React.Component{
    render(){
        return(
            <View>
                <Text> Message </Text>
            </View>
        );
    }
}